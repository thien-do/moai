{
	"name": "@moai/root",
	"scripts": {
		"prepare": "husky install && husky add .husky/pre-commit 'npx lint-staged'"
	},
	"lint-staged": {
		"*.{ts,tsx,js,css,json}": "prettier --write"
	},
	"devDependencies": {
		"husky": "^6.0.0",
		"lint-staged": "^10.5.4",
		"prettier": "^2.2.1"
	}
}
