{
	"extends": "../tsconfig.d.json",
	// Note that we don't generate from the source file! This is because some
	// how the typescript plugin
	"compilerOptions": { "outDir": "./dist" },
	"include": ["./src"]
}
