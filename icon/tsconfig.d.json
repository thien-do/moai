{
	"extends": "../tsconfig.d.json",
	// Note that we don't generate from the source file but the generated file
	"compilerOptions": {
		"allowJs": true,
		"outDir": "./dist"
	},
	"include": ["./dist/**/*.js"],
	"exclude": []
}
