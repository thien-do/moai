import { ProgressCircle } from "main/src/core";
import s from "./circle.module.css";

ProgressCircle.colors.inverse = {
  container: s.inverse,
  head: s.head,
  track: s.track,
};

ProgressCircle.colors.highlight = {
  container: s.highlight,
  head: s.head,
  track: s.track,
};

ProgressCircle.colors.neutral = {
  container: s.neutral,
  head: s.head,
  track: s.track,
};
